<%- include('partials/header') %>

<div class="card mb-3">
  <div class="card-body">
    <!-- for dipsplaying title and content -->
    <h1 class="card-title"><%= post.title %></h1>
    <p class="card-text"><%= post.content %></p>
    <!--creating buttons that link to the edit page and home page -->
    <a href="/" class="btn btn-secondary">Back to Home</a>
    <a href="/edit/<%= post.id %>" class="btn btn-primary ml-2">Edit Post</a>
    <!-- create a delete button, to delete posts when in posts route-->
    <!--<form action="/delete/<%= post.id %>" method="POST" onsubmit="return confirm('Delete this post?');">
      <button type="submit" class="btn btn-danger">Delete</button>
    </form>-->
  </div>
</div>

<%- include('partials/footer') %>
